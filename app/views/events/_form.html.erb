<%= form_with(model: event, local: true, url: user_events_path) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :day %>
    <%= form.select :day, select_days_options %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.select :time, select_times_options %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
